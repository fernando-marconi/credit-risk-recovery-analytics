# Copie este arquivo para ".env" e preencha os valores localmente.
# Nunca versione o arquivo ".env" (ja esta no .gitignore).

# Necessaria apenas para a camada opcional de IA generativa (src/genai_explainer.py)
GROQ_API_KEY=
